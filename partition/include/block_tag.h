#ifndef PARTITION_BLOCK_TAG_H_
#define PARTITION_BLOCK_TAG_H_

namespace partition {
/// @brief A `Cell` will either be in block A or block B.
enum class BlockTag { kBlockA, kBlockB };
}  // namespace partition

#endif  // PARTITION_BLOCK_TAG_H_
